# 11 — Reference binding: compiler clause + script-writer craft rule + config rows

**What to build:** A run with declared characters compiles a prompt that binds each
reference positionally with the probe-ratified clause wording (frozen verbatim in the
POV grammar config, evidence-tagged to probe artifacts), and the script stage never
appearance-describes a ref-bound subject in prose (appearance is owned by the image —
re-describing was the BUG-021 defect class). Craft rule enforced like existing ones:
structural check + one bounded repair. Also lands the probe round's config consequences:
evidence rows for what the probe validated, and the `style_register` action variant's
"No VFX aesthetic" negation scoped per probe evidence (it currently fights beam-VFX
beats — RESEARCH-slice2.md §3.C).

**Blocked by:** 09 (binding wording + config evidence come from watched probe rolls),
10 (reference plumbing exists). Sequence after 08 (same compiler file).

**Status:** ready-for-agent (once blockers close)

- [ ] Compiled prompt carries the frozen binding clause verbatim for each declared
      character, imageN numbering matching the gate's deterministic upload order
- [ ] Script whose prose appearance-describes a ref-bound subject triggers the bounded
      repair path; still-violating script fails loud
- [ ] POV grammar config gains the ref-binding rows with probe-artifact evidence tags;
      style_register scoping updated per probe verdict
- [ ] Zero-character runs compile byte-identical to before (no binding block, no
      behavior change)
