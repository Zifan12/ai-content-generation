# 12 — Validation run: first canon-IP post end-to-end

**What to build:** The operator's next post produced entirely by the pipeline — declared
character, gate-passed references, script, compiled ref-bound prompt, render sheet,
mandatory 480p sanity watch, ladder to finals, posted and logged with the existing
manual bookkeeping scripts. No hand-crafted prompt anywhere. This is slice ②'s
equivalent of slice ①'s first-live-run ticket: the first instance of the
protagonist-as-IP content class flowing through the machinery it validated.

**Blocked by:** 08, 09, 10, 11 — the full frontier.

**Status:** ready-for-human (operator picks story, watches, posts)

- [ ] One full run from `--idea`/`--topic` + `--character` to a watched 480p keeper with
      zero hand-edits to any artifact
- [ ] Watch checklist passes including the ref-specific failure modes (identity/costume
      match, style bleed, twins, protagonist-summoned-into-frame)
- [ ] Finals ladder followed (roll-until-clean + upscale path), post logged via
      record_post with view-count follow-up scheduled
- [ ] Operator-corrections log updated; any recurring defect promoted per whack-a-mole
      policy
