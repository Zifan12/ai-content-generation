# 09 — Probe A: protagonist-as-IP + beam register (manual, operator-gated)

**What to build:** Watched-render evidence for the four register unknowns no corpus can
answer (RESEARCH-slice2.md §2): (1) do costume crops bind to the POV camera-holder's
limbs, (2) does the costume hold without a start-frame anchor, (3) does an energy beam
render coherently in POV register, (4) does action-wrapped combat phrasing survive the
content filter. Hand-built on the slice-① probe skeleton — no pipeline code. Outcome
verdicts recorded probe-sheet style with artifact paths; these freeze ticket 11's binding
clause wording.

**Operator supplies (per the locked ref format spec, PRD-slice2 decision 2/5):** 2-4
crops of ONE version's clean official art showing what the camera sees — suit forearms/
gloves front + ¾, costume detail crop if the design has one. No face/mask panel, no
full-body sheet. Non-photographic official art only.

**Protocol:** 480p/10s, 30cr per roll, reroll-and-select 2-3 rolls, cap 120cr total
(includes one full-sheet A/B escalation if crops fail to bind — pre-registered, decision
2). Operator watches every roll — no sampled-frame verdicts. Failed/rejected jobs refund.

**Blocked by:** None — can start as soon as the operator drops source images.

**Status:** ready-for-human (operator sourcing + watch gates)

- [ ] Ref crops sourced by operator, one design version, logged with source provenance
- [ ] Probe prompt(s) hand-written on the probe-proven slice-① skeleton, binding clause
      candidates documented before spend
- [ ] Every roll watched by the operator; per-roll verdicts recorded against the four
      unknowns
- [ ] Evidence rows drafted for the POV grammar config (probe artifact paths as tags),
      ratified wording for the binding clause frozen — or a documented FAIL verdict with
      forensics if the register does not hold
- [ ] Total spend ≤120cr, stated before each roll
