# Research: i2i-generated/repaired refs for canon IP (2026-07-19)

Operator question: why not GPT Image 2 i2i (real still as input) to produce/clean canon refs?
Video-researcher full-read verdict (13 sources, file:line cited in session task output).

## Verdict: conditionally sanctioned — but re-source beats repair for canon IP

- The 2026-07-15 multi-view indictment hit the SHEET FORMAT (competing faces → one person
  reads as several, doc 08:21,79-101), NOT generated provenance. Kieft's fix still generates
  the face panel with GPT Image 2, practitioner-verified on our stack (Kieft workflow:39-50,
  INDEX:57). "Generated = banned" is a misreading — do not repeat it.
- Corpus discriminator (assembled, MED): identity-NEUTRAL edits (style pre-convert doc
  08:52-60 w/ explicit preserve-X rail, backgrounds, text removal) = sanctioned i2i class.
  Identity-BEARING regeneration (proportions, design geometry, fine costume) = re-source;
  invented pixels become canon (playbook:51). Canon-IP practiced precedent = unmodified
  crops + reject-and-resource, zero i2i instances (ip_probe recipe, HIGH).
- Repeated GPT-edit loops degrade the image — Kieft uses Photoshop for structural ref edits,
  explicitly NOT GPT re-edits (Kieft:54-56). Vendor "edit loops are safe" claim
  (02-image-prompting:83-88) is GPT Image 1.5 + capability-doc tier — practitioner evidence
  governs (INDEX:149).
- Per-defect routing for the godzilla set: version-mixing → reject at sourcing (HIGH);
  low-res softness → higher-res source first (playbook:157, HIGH); cutout fringe → UNCOVERED,
  nearest rule = deterministic tool; tone mismatch → sourcing-time matching only (guide:398).
- In-frame fidelity rule CONFIRMED distinct from POV-limb crops: visible character = headshot
  + full-body, exactly ONE image carries a face (L9 + Kieft reconciliation); limbs-only crops
  are the protagonist-role recipe (L11). Back view earns a slot only when design demands
  (playbook) — Godzilla dorsal plates qualify IF face-count stays 1.

## Consequence for refs/godzilla/ (current 3-file set)

Violations: faces in 01 AND 02 (competing faces); 01/02 web-cutout fringe + softness;
01/02 grey-green vs 03 navy tone; suspected 2014-vs-2019 plate-design mixing (02 vs 03).
Replacement recipe: 2 refs — (1) sharp official head close-up (THE face image), (2) official
full-body at face-less/rear-¾ angle covering dorsal plates, same design era + matched tone.
Re-gate + provenance table before next render.
